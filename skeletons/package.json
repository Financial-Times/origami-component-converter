{
  "name": "COMPONENT_NAME",
  "version": "COMPONENT_VERSION",
  "description": "COMPONENT_DESCRIPTION",
  "component": "COMPONENT_ORIGAMI_NAME",
  "main": "index.js",
  "module": "index.mjs",
  "source": "main.js",
  "scripts": {
    "compile": "npx buble -i ./src/js -o ./mjs --no modules; npx babel mjs --out-dir js --plugins=@babel/transform-modules-commonjs",
    "bundle": "../../node_modules/.bin/microbundle -f es,cjs --compress=false --target=web",
    "build": "npm run-script -s compile; npm run-script -s bundle"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.5",
    "@babel/plugin-transform-modules-commonjs": "^7.1.0",
    "buble": "^0.19.6"
  }
}
